# bun
export PATH="$HOME/.bun/bin:$PATH"

# copy to .envrc.local and fill in your keys
# export ANTHROPIC_API_KEY=sk-ant-...
# export ANTHROPIC_MODEL=opus

# load local overrides if present
[ -f .envrc.local ] && source .envrc.local
