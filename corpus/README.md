# corpus

test material for brane. real enough to extract. wrong enough to catch.

`code/` — services that do things they shouldn't.
`prose/` — writing that says things it shouldn't.

run brane against these. if it finds nothing, it's broken.
